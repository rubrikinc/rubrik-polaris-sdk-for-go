query RubrikPolarisSDKRequest($input: GetClusterNtpServersInput!) {
  result: clusterNtpServers(input: $input) {
    data {
      server
      symmetricKey {
        key
        keyId
        keyType
      }
    }
  }
}
