mutation RubrikPolarisSDKRequest(
    $clusterId: UUID!,
    $cloudType: CloudType!
) {
    result: disconnectExocomputeCluster(input: {
        clusterId: $clusterId
        cloudType: $cloudType
    })
}
