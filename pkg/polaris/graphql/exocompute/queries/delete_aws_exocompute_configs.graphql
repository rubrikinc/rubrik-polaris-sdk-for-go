mutation RubrikPolarisSDKRequest($configId: UUID!) {
    result: deleteAwsExocomputeConfigs(input: {
        configIdsToBeDeleted: [$configId]
    }) {
        deletionStatus {
            success
        }
    }
}
