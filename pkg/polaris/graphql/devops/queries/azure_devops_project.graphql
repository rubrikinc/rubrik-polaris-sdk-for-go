query RubrikPolarisSDKRequest($workloadId: UUID!) {
  result: azureDevOpsProject(workloadId: $workloadId) {
    id
    nativeId
    name
    orgId
    orgName
    url
    repoCount
    objectType
  }
}
