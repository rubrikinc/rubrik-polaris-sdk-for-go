query RubrikPolarisSDKRequest($workloadId: UUID!) {
  result: azureDevOpsRepository(workloadId: $workloadId) {
    id
    name
    orgId
    orgName
    projectId
    projectName
    url
    size
    objectType
  }
}
