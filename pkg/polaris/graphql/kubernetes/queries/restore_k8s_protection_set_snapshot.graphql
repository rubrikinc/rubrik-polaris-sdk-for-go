mutation RubrikPolarisSDKRequest(
    $id: String!,
    $jobConfig: RestoreK8sProtectionSetSnapshotJobConfigInput!) {
    result: restoreK8sProtectionSetSnapshot(
        input: {
            id: $id,
            jobConfig: $jobConfig
        }
    ) {
        status
        startTime
        progress
        nodeId
        links {
            rel
            href
        }
        id
        error {
            message
        }
        endTime
    }
}
