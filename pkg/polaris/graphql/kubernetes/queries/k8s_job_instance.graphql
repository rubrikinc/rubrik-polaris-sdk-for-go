query RubrikPolarisSDKRequest($k8sJobId: String!, $clusterUuid: UUID!) {
    result: k8sJobInstance(k8sJobId: $k8sJobId, clusterUuid: $clusterUuid) {
        id
        eventSeriesId
        startTime
        endTime
        jobStatus
    }
}
