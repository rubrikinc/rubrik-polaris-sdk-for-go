query RubrikPolarisSDKRequest($feature: CloudAccountFeatureEnum = CLOUD_NATIVE_PROTECTION) {
    azureCloudAccountPermissionConfig(feature: $feature) {
        permissionVersion
        rolePermissions {
            excludedActions
            excludedDataActions
            includedActions
            includedDataActions
        }
    }
}
