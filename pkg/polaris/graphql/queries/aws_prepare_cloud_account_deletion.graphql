mutation RubrikPolarisSDKRequest($cloud_account_uuid: UUID!, $feature: CloudAccountFeatureEnum!) {
    prepareAwsCloudAccountDeletion(input: {cloudAccountId: $cloud_account_uuid, feature: $feature}) {
        cloudFormationUrl
    }
}
