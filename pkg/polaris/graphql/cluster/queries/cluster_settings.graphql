query RubrikPolarisSDKRequest($id: UUID!) {
  result: cluster(clusterUuid: $id) {
    id
    name
    version
    status
    status
    timezone
    rawAddress
    geoLocation {
      address
    }
    ipmiInfo {
      isAvailable
      usesHttps
      usesIkvm
    }
  }
}
