query RubrikPolarisSDKRequest($fid: UUID!) {
    azureNativeSubscription(fid: $fid) {
        id
        nativeId
        name
        status
        slaAssignment
        configuredSlaDomain{
            id
            name
        }
        effectiveSlaDomain{
            id
            name
        }
    }
}
